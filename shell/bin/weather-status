#!/bin/bash

if [[ "$1" == "i3blocks" ]]
then
    curl -Ss 'https://wttr.in?0&T&Q' \
        | cut -c 16- \
        | head -n 2 \
        | xargs echo \
        | tr '[:upper:]' '[:lower:]'
    printf "\n#8be9fd\n"
else
    curl -Ss 'https://wttr.in?0'
fi
